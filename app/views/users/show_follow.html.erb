<div class="container">
  <% provide(:title, @title) %>
  <div class="text-center">
    <h1><%= @title %></h1>
  </div>
  <ul class="users">
    <%= render @users %>
  </ul>
  <%= paginate @users %>
</div>
