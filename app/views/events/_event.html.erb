<%= link_to event.event_name, event %>
<%= image_tag event.image.variant(resize:'200x200').processed if event.image.attached? %>
<p><%= event.event_name %></p>
<p><%= event.event_start_at %></p>
<p><%= event.created_at %></p>
